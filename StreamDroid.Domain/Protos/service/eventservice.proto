syntax = "proto3";

import "common/empty.proto";
import "common/event.proto";

service GrpcEventService {
  rpc Subscribe(grpc.model.Empty) returns (stream EventResponse);
}

message EventResponse {
  grpc.model.NotificationEvent event = 1;
}